 /*! clearStyle.min.js (c)2013 guest271314 http://guest271314.com 
  * License: http://www.opensource.org/licences/MIT 
  * Modified: 2013-12-10 Added: JavaScript/DOM implementation
  */
(function clearStyle(clear) {
    var clear = (!window.jQuery 
    ? 
    /*! JavaScript */
    (function clearStyle(s, i, clr) {
        var style = document.querySelectorAll("style");
        while (i < style.length) {
            style[i++].textContent = clr
        };
        var links = document.querySelectorAll("link");
        while (i < links.length) {
            links[i++].href = clr
        };
        var els = document.querySelectorAll("*");
        while (i < els.length) {
            els[i++].setAttribute("style", clr)
        };
        return (s === undefined ? "styles off" : s)
    }(undefined, 0,"")) 
    : 
    /*! jQuery (Depends: http://code.jquery.com) */
    (function clearStyleJq(s,clr) {
        $("link[rel=stylesheet]").attr("href", clr);
        $("style").html(clr);
        $("*").attr("style", clr);
        return (s === undefined ? "styles off" : s)
    }(undefined,""))
    );
    return (clear === undefined ? "styles off" : clear)
}());
