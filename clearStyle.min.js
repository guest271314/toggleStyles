/*! clearStyle.min.js (c)2013 guest271314 http://guest271314.com 
 * License: http://www.opensource.org/licences/MIT 
 * Modified: 2013-12-08 Added: JavaScript/DOM implementation
 */
(function clearStyle(clear,clr) {
"use strict";
    (!window.jQuery     
    ? 
    /*! JavaScript */
    (function clearStyleJs() {
        var els = document.querySelectorAll('*');
        var links = document.querySelectorAll('link[rel=stylesheet]');
        var styles = document.querySelectorAll('style');
        for (var i = 0; i < els.length; i++) {
            els[i].setAttribute('style', clr);
            links[i].setAttribute('href', clr);
            styles[i].textContent = clr;
        }
    }())     
    : 
    /*! jQuery (Depends: http://code.jquery.com) */
    (function clearStyleJq() {
        $('link[rel=stylesheet]').attr('href', clr);
        $('style').html(clr);
        $('*').attr('style', clr);
    }()));
    /*! return `styles off` for undefined */
    return (clear === undefined ? 'styles off' : clear)
}(undefined,''))
